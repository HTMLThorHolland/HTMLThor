	
<html>
<head>
</head>
<body>
		<!-- This is the encapsulation function designed to check if tags inside other -
		---- tags is incorrect and returns any appropriate errors --------------------->
		
		/*
		 *	input(tag) = each tag in the file that is added to a stack (or something 
		 *				 similar)
		 *	output(string) = any error that comes from having tags inside 
		 *								other tags
		 */
		
		encapsulation(tag) {
		
		
		//return 0 if no errors
		
		// return string of error message if errors, and type of error
		}
		
		
		
</body>
</html>
		 